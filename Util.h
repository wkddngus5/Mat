#include <stdio.h>
#include<stdlib.h>

void readMat(Mat* matPtr, int count);

void searchMat(Mat* matPtr, int count, int* changeContents);

void writeMat(Mat* matPtr, int* pcount, int* changeContents);